import React from 'react'\nimport { render, screen, fireEvent } from '@testing-library/react'\nimport TransferConfirmationModal from '../../../components/transfer/TransferConfirmationModal'\ntest('shows transaction id and closes', () => {\n  const handleClose = jest.fn();\n  render(<TransferConfirmationModal open={true} transactionId='1234' onClose={handleClose} />);\n  expect(screen.getByText(/1234/)).toBeInTheDocument();\n  fireEvent.click(screen.getByText(/Close/i));\n  expect(handleClose).toHaveBeenCalled();\n});\n