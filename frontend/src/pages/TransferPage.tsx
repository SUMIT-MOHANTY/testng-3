import React, { useState } from 'react'\nimport TransferForm from '../components/transfer/TransferForm'\nimport TransferConfirmationModal from '../components/transfer/TransferConfirmationModal'\nimport { submitTransfer } from '../api/transferService'\nimport { Container, Typography } from '@mui/material'\nimport '../../styles/transfer.css'\nexport default function TransferPage() {\n  const [txnId, setTxnId] = useState('');\n  const [modalOpen, setModalOpen] = useState(false);\n  const handleSuccess = async (data: any) => {\n    const resp = await submitTransfer(data);\n    setTxnId(resp.transactionId);\n    setModalOpen(true);\n  };\n  const handleClose = () => setModalOpen(false);\n  return (\n    <Container className='transfer-page'>\n      <Typography variant='h4' gutterBottom>Funds Transfer</Typography>\n      <TransferForm onSubmit={handleSuccess} />\n      <TransferConfirmationModal open={modalOpen} transactionId={txnId} onClose={handleClose} />\n    </Container>\n  );\n}\n