import React from 'react'\nimport { Dialog, DialogTitle, DialogContent, Button } from '@mui/material'\ntype Props = { open: boolean; transactionId: string; onClose: () => void }\nexport default function TransferConfirmationModal({ open, transactionId, onClose }: Props) {\n  return (\n    <Dialog open={open} onClose={onClose} aria-labelledby='transfer-confirmation-title'>\n      <DialogTitle id='transfer-confirmation-title'>Transfer Completed</DialogTitle>\n      <DialogContent>\n        <p>Your transaction was successful.</p>\n        <p><strong>Transaction ID:</strong> {transactionId}</p>\n        <Button onClick={onClose} variant='contained' color='primary'>Close</Button>\n      </DialogContent>\n    </Dialog>\n  );\n}\n