parameters:\n  dockerfile: ''\n  imageName: ''\n  tag: ''\nsteps:\n- template: templates/login-acr.yml\n- script: |\n    docker build -f ${{{{ parameters.dockerfile }}}} -t ${{{{ parameters.imageName }}}}:${{{{ parameters.tag }}}} .\n  displayName: 'Docker Build'\n- script: |\n    docker push ${{{{ parameters.imageName }}}}:${{{{ parameters.tag }}}}\n  displayName: 'Docker Push'\n